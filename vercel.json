{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "frontend/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)**/*.md' ':(exclude)**/*.txt'"
}
